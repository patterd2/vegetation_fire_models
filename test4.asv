%interp2(reshape(locations(:,1),[1,num_sites]), ...
        %reshape(locations(:,2),[1,num_sites]), Temp_Sol(:, i), 0:dx:L, 0:dy:L)

test = reshape(locations(:,1),[1,num_sites]);

X = zeros(num_sites);
Y = zeros(num_sites);

for i
interp2(X, Y, Temp_Sol(:, i), 0:dx:L, 0:dy:L)